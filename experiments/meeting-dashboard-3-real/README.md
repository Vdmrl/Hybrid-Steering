# Real Experiment 3 dashboard

This is the corrected Dashboard 3. It is built from the completed
`composition-normalization-v3/summary.json`, not from the older factorial-run
used by the first draft of Dashboard 3.

```powershell
python experiments/meeting-dashboard-3-real/build.py
```

The output is `outputs/meeting-dashboard-3/index.html`. The committed JSON is
a compact summary only; raw generations and Judge artifacts stay outside Git.
