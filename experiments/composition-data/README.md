# Composition data

Compact, strictly filtered donor pairs and held-out prompts reused by
composition experiments. The original legacy runner and pre-v2 Judge outputs
are intentionally not retained; new evaluations use the shared Judge v2.
