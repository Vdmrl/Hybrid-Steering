# Atomic pair screen

Four-prompt smoke test for:

- atomic sentences + optimism;
- atomic sentences + numbered list.

Each run generates baseline, both singletons, and the RSS+clamp pair using the
previously selected singleton ranks and alpha values. No Judge API is used.
